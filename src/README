Tool per estrapolare informazioni utili da form da "prolon.it".



Istruzioni per il packaging:
    -Per fare il packaging di ParseScript in un file *.exe (o un eseguibile per i sistemi Unix) leggere i commenti "ON PACKAGING" ed utilizzare il tool pyinstaller (pyinstaller --onefile --windowed --icon=\path\to\icon main.py) dalla directory root del progetto perché per settare le icone si utilizzano paths relativi.
    Visitare li sito https://wxpython.org/pages/downloads/index.html per installare il package wxpython.

Istruzioni per eseguire lo script in sistemi Unix:
    -Eseguire il file ParseScript/support/setup_scripts/linux/setup.sh (crea una desktop entry per i sistemi UNIX copiando la cartella ParseScript/ in /opt/ e il file ParseScript/support/setup_scripts/linux/parsescript.desktop nella directory /usr/share/applications)
    Cercare in ParseScript/support/libs/linux/ per librerie mancanti.
    Visitare li sito https://wxpython.org/pages/downloads/index.html per installare il package wxpython.
